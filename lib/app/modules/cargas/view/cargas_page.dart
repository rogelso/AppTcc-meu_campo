import 'package:flutter/cupertino.dart';

class CargasPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Tela das Cargas'),
    );
  }
}
